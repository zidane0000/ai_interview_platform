# Mock Environment Mode
# This mode runs the frontend with mock data, useful for development and testing
# without requiring a backend server.

VITE_API_BASE_URL=http://localhost:8080
VITE_USE_MOCK_DATA=true
VITE_DEV_MODE=true

# App settings
VITE_APP_TITLE=AI Interview Platform (Mock Mode)
VITE_APP_VERSION=1.0.0
